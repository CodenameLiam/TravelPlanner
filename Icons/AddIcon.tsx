import React from "react";
import { Ionicons } from "@expo/vector-icons";
import { DARKBLUE, MIDBLUE } from "../Styles/Colours";

export default function AddIcon() {
    return <Ionicons name="add-circle" size={20} color={DARKBLUE} />;
}
